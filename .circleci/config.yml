# PHP CircleCI 2.0 configuration file# PHP CircleCI 2 
#
# Check https://circleci.com/docs/2.0/language-php/ for more details
#
version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/php:7.1.5-browsers
      
      # Specify service dependencies here if necessary
      # CircleCI maintains a library of pre-built images
      # documented at https://circleci.com/docs/2.0/circleci-images/
      # - image: circleci/mysql:9.4

    working_directory: ~/repo

    steps:
      - checkout

      # Download and cache dependencies
      - restore_cache:
          keys:
          - v1-dependencies-{{ checksum "composer.json" }}
          # fallback to using the latest cache if no exact match is found
          - v1-dependencies-

      # - run: composer install -n --prefer-dist

      - save_cache:
          paths:
            - ./vendor
          key: v1-dependencies-{{ checksum "composer.json" }}
        
      # run tests!
      - run: 
          name: "Testes"
          command: vendor/phpunit/phpunit/phpunit Teste
      - run: 
          name: "Install Deploy"
          command: curl -LO https://deployer.org/deployer.phar ; chmod +x deployer.phar;
      - add_ssh_keys:
          fingerprints:
            - "e4:6c:d3:e4:d5:1c:ca:39:26:2b:ec:9a:21:dd:74:4c"
      - add_ssh_keys:
          fingerprints:
           - "18:58:c5:8a:df:93:f0:75:ae:9b:53:a4:d3:90:b5:d6"
      - run:
          name: "Deploy"
          command: cat ~/.ssh/config
          command: ./deployer.phar deploy
